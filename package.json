{
  "name": "gitpatch-viewer",
  "version": "1.1.0",
  "description": "Offline viewer for unified diff patches",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --outdir=dist && cp src/index.html src/_headers dist/",
    "dev": "esbuild src/app.js --bundle --outdir=dist --watch",
    "diff": "mkdir -p .fixtures && git diff HEAD > .fixtures/working.diff && echo 'wrote .fixtures/working.diff'",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "keywords": [],
  "dependencies": {
    "diff": "^9.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
